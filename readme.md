# MKWii Discord RPC

## how to use

- clone the repo
- setup a venv

```sh
python -m venv .venv && source .venv/bin/activate
```

- install deps

```sh
pip install -r requirements.txt
```

- run main.py

```sh
python main.py
```